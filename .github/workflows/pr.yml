name: Pull Request

on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - run: npm ci
      - uses: bartveneman/project-wallace-github-action@master
        with:
          project_wallace_token: "SUPER_S3CR3T_T0K3N!"
